# 《改革宗系统神学》中文译本审校修订流程 SOP

> 用途：对多卷神学译著进行「NotebookLM 逐页审校 + 应用到 Word + 产出最终版」的完整操作手册。
> 适用范围：每一卷（如第二卷《人论与基督论》、第三卷《圣灵与救恩》、第四卷《教会与末后之事》）。

---

## 一、完整流程（按阶段）

### 阶段 0 · 准备
1. 用户把该卷**中文 PDF** 和**英文原版**（纯文字版 docx）放进卷文件夹（如 `新建文件夹\第二卷`）。
2. 用户把 **NotebookLM 资料库切换成该卷**（上传英文原版 + 中文译本）。
3. 确认文件夹内容；用 pypdf 查 PDF 页数，据此算出批次。

### 阶段 1 · PDF → Word 转换
- 用 **pdf2docx** 把中文 PDF 转成同名 `.docx`（约 10 分钟/千页，后台跑）。
- 依据页数算批次：`1-50, 50-100, 100-150 ...` 直到覆盖全书（50 页一进，边界共享）。

### 阶段 2 · 写驱动脚本 batch_proofread_vX.py
- 内置**精确提示词模板（长版）**（见下方「提示词模板」），页数占位 `[1]/[50]`，运行时替换成实际页数。
- 每批：提交提示词(RPA) → **等 5 分钟** → 复制回答 → 追加进「审核第X卷.docx」。
- 关键防错：prompt-echo 检测（复制到提问原文=答案没生成完→重试）、断点续跑（按 docx 已有「第 X-Y 页」跳过）、UTF-8 输出、手机 ntfy 通知。

### 阶段 3 · 收集审校
- 后台跑驱动，约 2 小时/卷；每批自动存进「审核第X卷.docx」。
- 可暂停/重启不丢进度；完成后通知用户。

### 阶段 4 · 解析审校发现
1. `_extract_docx.py` 把「审核第X卷.docx」+ 中文 Word 版提取成 txt。
2. 找批次边界行号。
3. 用 **Workflow 并行子代理**（约 11 个）逐批解析，输出 JSON：`find`（错误译法，去中文版 grep 核实）、`replace`（正确译法）、`change_type`（global/contextual）、`present_in_docx`、`occurrences`。
4. 合并去重。

### 阶段 5 · 人工审查清单
- 导出 `_changes_vX.json` + 审查清单（风险项置顶）。
- 剔除误报；修正过宽的全局替换；处理 find/replace 嵌套冲突；跨卷一致性覆盖。

### 阶段 6 · 应用修改
- `_apply_vX.py`：run 感知替换（只改命中的文字、保留段落格式）；替换后 pos 前移避免自我嵌套；保护短语机制（书名等先占位符再还原）。
- 存为 `..._最终版.docx`。

### 阶段 7 · 验证
- 检查占位符泄漏、find 残留（排除「find 是自己替换结果子串」误报）、关键术语抽查，旧词残留应为 0。

### 阶段 8 · 跨卷一致性统一
- 扫描所有卷，找同词不同译（如规管/限定、圣道执事/牧师、保证人/担保人、信仰告白/信条），统一为多数/既定译法。
- ⚠️ Word 打开的文件会被锁，需先关闭才能保存。

### 阶段 9 · 归档
- 建「中文版最终版本」文件夹：放各卷最终版 + 各卷「审核第X卷.docx」。
- 用 `shutil.make_archive` 压缩成 zip（中文文件名正常编码）。

---

## 二、提示词模板（统一使用长版，一字不差）

> **所有卷统一使用以下长版提示词**（第一卷历史上一度用短版，现已统一为长版）。
> 使用时把 `[1]`、`[50]` 替换为实际页数；每批 50 页一进，边界共享。
> 此模板已按用户要求把「一百页」改为「五十页」。一字不改、原样发送。

```text
你现在是一位精通英文和中文，并且深谙"改革宗系统神学"与"长老制教会"历史、教义和专属词汇的顶级神学翻译审校专家。你熟悉《海德堡要理问答》（Heidelberg Catechism）、《韦斯敏斯德信条》（Westminster Confession of Faith）以及《多特信经》（Canons of Dort）等核心改革宗历史信条的中文标准译法。


我已在知识库中上传了周毕克（Joel Beeke）《改革宗系统神学》（Reformed Systematic Theology）的英文原版和中文译本。


【当前任务】
由于中文版篇幅较长，我们将按照页码区间进行推进。请你严格按照以下标准，仅针对 中文译本的第 [1] 页至第 [50] 页 的内容进行细致梳理，并自动在英文原版中定位其对应的段落，进行逐段比对和深度审校。请绝对不要越界分析规定页码范围之外的内容。


【审校标准】



核心术语审查：检查中文翻译是否偏离了改革宗长老会的标准神学术语（例如：将"Justification"误译、将"Covenant of Grace"表达不准确、或者圣礼、教会体制相关的词汇翻译不符合长老会背景）。

神学语境准确性：检查译文是否在神学逻辑、教义推演上违背了作者原意，或者削弱了原有的神学严谨性。

漏译与过度意译：找出丢失了重要神学限定词的句子，或过度通俗化导致神学意味丧失的段落。


【输出格式要求】
为了方便我后续将其整理为正式的审校文档，请务必以严谨的"结构化报告"形式输出你的发现。请将查出的每一个问题按照以下格式列出（重点关注翻译不准确、神学表达不到位的地方，请务必在这五十页的范围内，至少挖掘并列出 5 到 10 个核心修改建议）：


翻译审校报告：中文版第 [1] - [50] 页

1. 术语/短语不当或错误翻译（所在中文页码：[具体出现问题的页码]）



英文原文: [提取英文原版中对应的原句]

当前中文译文: [提取知识库中对应的中文译本内容]

修改建议: [提供符合改革宗长老会背景的精准翻译]

神学与语境分析: [详细说明为什么现有的翻译不合适？它在改革宗神学背景下可能引发什么误解？结合《海德堡要理问答》或相关信条的用词习惯进行解释。]


2. [按照上述格式继续列出下一个问题点]
...


【执行指令】
请先确认你已经锁定了目标中文页码范围，并在英文版中找到了对应内容。深呼吸，进行细致的交叉比对，最后输出上述的审校报告。报告内容请尽可能详实、专业，字数要充足，以达到直接作为修订参考文档的质量。

```

---

## 三、踩过的坑（重点）

| 坑 | 后果 | 解决 |
|---|---|---|
| 等待时间太短 | 复制到「提示词回显」而非答案 | 加 prompt-echo 检测自动重试 |
| 控制台 GBK | ✓ 等字符导致脚本崩溃 | 脚本开头 reconfigure 为 UTF-8 |
| 直接替换段落文本 | 毁掉段落排版 | run 感知替换 |
| replace 含 find | 无限嵌套 | 替换后搜索位置前移 |
| Word 打开文件 | 无法保存 | 需用户先关闭 |
| 不同卷报告相反 | 保证人/担保人方向相反 | 用户拍板统一 |

---

## 四、可复用脚本

- `_extract_docx.py`：docx → txt
- `batch_proofread_vX.py`：审校驱动
- `_export_vX.py`：导出 findings
- `_apply_vX.py`：应用修改（run 感知）
- `_verify_vX.py`：验证
- `_unify_cross_volume.py`：跨卷统一
- `_append_batch.py`：手动模式存批次
- `手机通知推送.py`：ntfy 手机通知

## 五、时间估算（每卷）
PDF 转换 ~10 分钟 + 收集 ~5.5 分钟/批 × 批数 + 解析 ~8 分钟 + 应用/验证秒级 ≈ **每卷 2.5-3 小时**（主要花在收集）。
