# 《改革宗系统神学》审校策略总纲：方法一（主力）+ 方法二（精修）

> 本文档确定两套审校方法的**分工与接力关系**：
> **方法一（NotebookLM 逐页审校）为全书主力**，负责全覆盖审校；
> **方法二（LF Aligner + Gemini 对齐审校）为精修工具**，负责方法一之后的术语级专项精修与跨章统一。
> 两者不是二选一，而是前后衔接。依据为 2026-08 在第四卷第 1 章切片上的端到端实测。

---

## 一、决策结论

| 定位 | 方法 | 职责 |
|---|---|---|
| **主力** | 方法一 · NotebookLM 逐页审校（`审校流程SOP.md`） | 全书覆盖审校：漏译、过度意译、神学语境逻辑错误 |
| **精修** | 方法二 · LF Aligner + Gemini 对齐审校（`审校流程SOP-方法二…md`） | 术语级专项核对、跨章统一、引用/标注格式检查 |

**一句话**：方法一负责"全书每一页都过一遍"，方法二负责"在方法一的基础上，对具体术语做可核实的精准精修"。

---

## 二、分工依据

### 2.1 方法一为什么当主力

1. **全覆盖**：按 50 页一批驱动，整本书每一页都被 NotebookLM 审到，不存在"某些行根本没被看"的情况。
2. **成熟度**：已实际跑完第二、三、四卷，SOP 中沉淀了全部踩坑修复（prompt-echo 检测、GBK 控制台、run 感知替换防排版损坏、find/replace 嵌套、Word 锁文件、跨卷相反结论），流程可靠可重复。
3. **审校能力匹配**：NotebookLM 知识库中同时有英文原版与中文译本，能做**整页上下文对照**——最擅长抓方法二按行审最容易漏的三类问题：漏译、过度意译、神学逻辑/教义推演错误。
4. **吞吐可预测**：约 2.5–3 小时/卷，主要耗时在等待（每批约 5 分钟），驱动脚本支持断点续跑，可暂停重启不丢进度。

### 2.2 方法二为什么只能当精修（2026-08 实测证据）

在第四卷切片（docx 第 152–700 段，约全书 7%）上端到端跑通方法二，得到的硬数据：

| 环节 | 实测结果 |
|---|---|
| 对齐 | 段落级对齐 **97%** 正确；句级对齐仅 **47%**（SOP 现行参数 `--segment="y"` 是错的，须改 `"n"`） |
| 词汇库预筛 | 26 条候选，**25 条是同形误报**；Gemini 真正抓到的好错误（世俗→民事、操练→执行）**都不在词库错词列表上**，能进候选纯属巧合 |
| 漏检 | 其余 22 条未送审候选里，**6 条（27%）带真实错误**完全没被看到（见下） |
| Gemini 精度 | 送审的 4 条**全部命中真错误（4/4）**，诊断方向全对、引文联网可核实；但建议词只有 2/4 完全恰当，且漏了同行内的次生错误 |

方法二漏掉的真错误类型（全部是"整页/跨句上下文"才能抓到的）：
- 丛书缩写 RST 被误译成《修订标准译本》（3 处）
- 教父文献缩写 NPNF1 被误译成《新约全集》（2 处）
- 希伯来语词 qahal 被误标注为"希腊文"
- 丢词 `partly`、多出"象征"（弱化圣餐的"是相交"为"是象征"）
- 章节号引用错误

**结论**：方法二在"它被允许看到的内容"里非常准，但视野被词汇库预筛限制住了——预筛召回差，真错反而多在被筛掉的行里。因此方法二**不能当主力做全覆盖**，但它在术语级精修上（引文可核实、跨章可聚合、标准译法可核对）有方法一替代不了的价值。

---

## 三、合并工作流（接力关系）

### 阶段 0 · 准备（两方法共享基础）

- 共用文件：英文原版 `EN.docx`、中文译本 `ZH.docx`、术语库 `Glossary.csv`
- 方法一额外需要：中文 PDF、NotebookLM 资料库切换（上传英文本+中译本）、pypdf 算批次
- 方法二额外需要：LF Aligner（装于中文路径无碍，但输入文件须放 ASCII 目录如 `C:\LF_Aligner\vol4\`）、`ask_gemini.py` 模拟点击脚本、`get_coords.py` 校准坐标

### 阶段 A · 方法一：主力全覆盖审校（沿用现有 SOP）

1. pdf2docx 转 Word（后台，约 10 分钟/千页），按 50 页一批计算批次
2. `batch_proofread_vX.py` 驱动 NotebookLM：每批提交长版提示词 → 等 5 分钟 → 复制回答 → 追加进「审核第X卷.docx」（prompt-echo 检测 + 断点续跑）
3. `_extract_docx.py` + Workflow 子代理逐批解析 → `_changes_vX.json` + 审查清单
4. 人工审查清单：剔除误报、修正过宽替换、处理嵌套冲突、跨卷覆盖
5. `_apply_vX.py` 应用到中文版 → `最终版.docx`（run 感知替换，保留排版）
6. `_verify_vX.py` 验证（find 残留为 0、占位符无泄漏）

**产出**：`最终版.docx`（方法一全覆盖审校完成）

### 阶段 B · 方法二：术语级精修（在方法一最终版基础上）

1. 用 LF Aligner **段落级对齐** EN/ZH（`--segment="n"`，勿用 `"y"`），产出 `Aligned_vX.xlsx`
2. **词汇库初筛（清洗后）**：`_batch_audit_vX.py` 扫中文列命中错译 + 同段英文含原词 → 候选
3. **Gemini 语境审校**：通过 `ask_gemini.py` 把候选（建议**全部对齐行分批发**，而非只送预筛行）连同章节标题、批次边界共享上下文提交 Gemini，提示词约定"英文为锚、中文仅作对照"
4. 导出 `_Audit_Report_vX.xlsx`（英文原句 | 中文译句 | 当前错译 | 建议标准译法 | 神学语境分析 | 人工确认）
5. 人工在 Excel 打勾，剔除误报与过度意译
6. `_apply_vX.py` 应用到**方法一最终版** → `精修最终版.docx`

**产出**：`精修最终版.docx`（方法一全覆盖 + 方法二术语精修）

### 阶段 C · 跨卷一致性统一 + 归档（两方法共用）

1. `_unify_cross_volume.py` 扫描各卷最终版，统一专有名词（如 世俗→民事 这类跨卷高频词，可用方法二的跨章聚合清单做全局替换依据）
2. 关闭 Word 进程后 `shutil.make_archive` 打包 ZIP 归档

---

## 四、方法二作为精修工具的三个必改点

> 方法二要真正"够格当精修"，以下三条缺一不可。其中 1、2 属于 SOP 与词库修正，3 属于送审策略。
> **（状态：三条已于 2026-08 落实进《审校流程SOP-方法二…md》并推送，提交 `2b40833`；以下即当前 SOP 的要求。）**

1. **对齐参数修正**：SOP 中 `--segment="y"`（句级）须改为 `--segment="n"`（段落级）。实测句级 47% vs 段落级 97%，句级对齐错位会让后续审校建立在错误对应上。
2. **词汇库清洗与增补**：
   - 删除同形误报词：宣告、恢复、证明、誓言、会议、注释、长老会、法令等（这些在日常语境正确，作为错词匹配只会制造假阳性）
   - 增补可模式化检查项：丛书缩写（RST、NPNF 等）、语言标注（希伯来/希腊文）、章节号引用——这三类正是实测漏检的大头
3. **送审范围扩大**：不要只送词汇库预筛行。实测预筛 26 条里 25 条误报、而真错在别处。精修时应**把全部对齐行分批发给 Gemini**（每批 50 对 + 章节上下文），预筛只用于给候选排优先级，不用于决定审不审。

---

## 五、审校目标 × 选用方法速查表

| 审校目标 | 用哪个 | 说明 |
|---|---|---|
| 全书覆盖（漏译/意译/神学逻辑） | 方法一 | 主力，每页都审 |
| 具体术语的标准译法核对 | 方法二 | 对齐行级 + 引文可核实（如威敏 23 章「民事长官」、海德堡 54 问「活泼的肢体」） |
| 跨章/跨卷术语统一 | 方法二 | 术语跨章聚合送审，给出全书出现处清单 |
| 缩写/标注/引用格式（RST、NPNF、qahal、章节号） | 方法二 | 需先补入词库的模式检查 |
| 排版保护与最终落地 | 共用 | 两方法最终都用 `_apply_vX.py`（run 感知替换） |
| 大批量收集审校意见 | 方法一 | NotebookLM 每批 50 页，吞吐稳定 |

---

## 六、试点与迭代建议

1. **先试点再推广**：方法二目前只在第四卷第 1 章切片（约 7%）验证过，全量未跑。建议先在**某一卷完整跑一遍"方法一全覆盖 + 方法二精修"**，确认精修环节的真实增益与耗时，再决定各卷是否统一采用。
2. **词库持续迭代**：方法二每次跑完，把 Gemini 发现的新高频错译补进 `Glossary.csv`（SOP 中已有此约定）。词库越厚，初筛与模式检查越准，精修消耗的 Gemini 调用越少。
3. **方法二权重可上调**：若试点显示精修增益大（术语级命中多、误报少），可评估把方法二从"精修"升格为与方法一并列的"第二遍全量审校"。

---

## 七、已知局限与诚实说明

- **方法一的真实产出质量缺硬数据**：本文档关于方法一的判断依据是其在第二、三、四卷的成熟运行记录；若手头有前几卷的「审核第X卷.docx」或 `_changes_vX.json`，可抽样核对方法一实际审出多少条、误报率多少，进一步校准"主力"定位。
- **方法二的数据来自单一切片**：4/4 精度、6/22 漏检等数字均来自第四卷第 1 章切片，样本小，正式采信前需试点卷全量复测。
- **方法一抓不到的、方法二也不一定抓得到**：两方法都以"中英对照"为核，若英文原版本身有错或中文 PDF 转 Word 丢字，需另行处理。
